<html>
  <head>
<title>TimING - <%= yield(:title) || "TimING" %></title>
		<%= stylesheet_link_tag('sessions' , :media => 'all') %>

  </head>
  <body>
<!-- <div id="user_nav">
  <% if current_user %>
    Logged in as <%= current_user.email %>.
    <%= link_to "Wyloguj", wyloguj_path %>
  <% else %>
    <%= link_to "Rejestruj", rejestruj_path %> or
    <%= link_to "Zaloguj", zaloguj_path %>
  <% end %>
</div> -->
<!-- 
<% flash.each do |name, msg| %>
  <%= content_tag :div, msg, :id => "flash_#{name}" %>
<% end %> -->
  <%= yield %>

  </body>
</html>
